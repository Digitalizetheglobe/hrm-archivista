<?php $lines = file("c:/xampp/htdocs/hrm_rising/app/Http/Controllers/PaySlipController.php"); $out = []; $capture = false; foreach($lines as $line) { if (strpos($line, "public function calculatePayableDaysFromStatusCodes") !== false) { $capture = true; } if ($capture && strpos($line, "public function salaryProcessing()") !== false) { break; } if ($capture) { $out[] = $line; } } file_put_contents("scratch_helpers.txt", implode("", $out));
